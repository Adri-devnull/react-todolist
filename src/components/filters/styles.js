import styled from 'styled-components';

const StyledContainersFilter = styled.div`
	width: 300px;
	color: #fff;
	background-color: #25273d;
	padding: 18px 13px;
	display: flex;
	align-items: center;
	justify-content: center;
	gap: 20px;
	border-radius: 10px;
`;

const StyledSpanFilter = styled.span`
	font-size: 0.8em;
	cursor: pointer;
	color: #c8cbe7;
	font-weight: bold;
`;

export { StyledContainersFilter, StyledSpanFilter };
